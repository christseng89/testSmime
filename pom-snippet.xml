<!-- Add these dependencies to your Java EE / Jakarta EE project's pom.xml -->
<!-- Versions are indicative; align with your platform's supported BOM.    -->
<dependencies>

    <!-- Jakarta Mail API (use javax.mail 1.6.x instead if still on Java EE 8) -->
    <dependency>
        <groupId>jakarta.mail</groupId>
        <artifactId>jakarta.mail-api</artifactId>
        <version>2.1.3</version>
    </dependency>

    <!-- Jakarta Mail implementation (Angus Mail) -->
    <dependency>
        <groupId>org.eclipse.angus</groupId>
        <artifactId>angus-mail</artifactId>
        <version>2.0.3</version>
    </dependency>

    <!-- Bouncy Castle: provider, PKIX/certs, and S/MIME -->
    <dependency>
        <groupId>org.bouncycastle</groupId>
        <artifactId>bcprov-jdk18on</artifactId>
        <version>1.78.1</version>
    </dependency>
    <dependency>
        <groupId>org.bouncycastle</groupId>
        <artifactId>bcpkix-jdk18on</artifactId>
        <version>1.78.1</version>
    </dependency>
    <dependency>
        <groupId>org.bouncycastle</groupId>
        <artifactId>bcmail-jdk18on</artifactId>
        <version>1.78.1</version>
    </dependency>

</dependencies>

<!--
  If you are on Java EE 8 (javax.* namespace) rather than Jakarta EE 9+,
  replace the two Jakarta Mail dependencies with:

    com.sun.mail:jakarta.mail:1.6.7   (provides javax.mail.*)

  and change the imports in SMIMEMailSender.java from jakarta.mail.* to javax.mail.*.
  The Bouncy Castle parts stay the same.
-->
